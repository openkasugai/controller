apiVersion: example.com/v1
kind: DataFlow
metadata:
  name: df-calcapp-sriov
  namespace: cpufunc-calcapp
spec:
  functionChainRef:
    name: calcapp-sriov-chain
    namespace: cpufunc-calcapp
  requirements:
    all:
      capacity: 1

